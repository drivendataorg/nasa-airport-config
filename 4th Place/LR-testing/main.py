import usage_preprocess_test as up

up.process_airports_departure_takeoff()
# at the end of this, we will get {airport}_landing_dist.csv and {airport}_take_off_dist.csv

# importing the file itself will run everything because I designed this poorly
import LR_preprocessing_testing

# At the end of this, we will have testing_data.csv

import test_data_boost

# and we are done
